# Sample applications

- `ios`: SwiftUI reference app using the local Swift package.
- `../android/sample-app`: Android reference app using the local Gradle modules.

Both apps cover the same flow: configure a session, record a WAV master, submit an
audio job, and refresh its status. Credentials entered in the sample UI are held in
memory only.
